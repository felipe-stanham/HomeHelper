# P-0004: Environment-Scoped App Services + Bootstrap Docs

**Created:** 2026-04-24 | **Status:** [IN PROGRESS]

Small fix-and-document project. No `Projects/P-0004/` spec package — the scope is narrow and self-describing.

## Background

The Latarnia platform already runs as two environment-scoped system-level systemd units on the homeserver:
- `latarnia-tst.service` (TST)
- `latarnia-prd.service` (PRD)

However, **per-app** services generated at runtime by `service_manager.py` use the prefix `latarnia-` with no environment segment. Because the Pi runs both TST and PRD under the same user, installing an app with the same name in both environments would produce two `~/.config/systemd/user/latarnia-{app}.service` files that collide.

Additionally, the `latarnia-tst.service` / `latarnia-prd.service` unit files are currently hand-installed on the Pi with no documented procedure for a fresh-host bootstrap.

## Scope Summary

| # | Scope | Branch | Status |
|---|-------|--------|--------|
| 1 | Env-Scoped Services + Bootstrap Docs | scope-P-0004-1-env-scoped-services | [IN PROGRESS] |

---

## Scope 1: Env-Scoped Services + Bootstrap Docs

**Branch:** `scope-P-0004-1-env-scoped-services`
**Status:** `[IN PROGRESS]`
**Depends on:** none

### Tasks
- [ ] Make `ServiceManager.service_prefix` environment-aware: read `ENV` from the environment, validate against `{dev, tst, prd}` (default `dev`, warn on anything else), and set `service_prefix = f"latarnia-{env}-"`.
- [ ] Update `tests/unit/test_service_manager.py` to cover the new default and the tst/prd variants.
- [ ] Update `.claude/skills/deployment-process/SKILL.md`:
  - Record the new per-app unit naming (`latarnia-{env}-{app}.service`).
  - Add a **First-Time Bootstrap** section with the full `latarnia-tst.service` / `latarnia-prd.service` templates, install commands, and `systemctl daemon-reload` / `enable` / `start` steps.
- [ ] Commit the pre-existing skill-folder rename (`skills/deployment/deployment_process.md` → `skills/deployment-process/SKILL.md`).
- [ ] Update `docs/SYSTEM.md` Projects table to list P-0004.
- [ ] **Blocking `[DONE]`:** Replace the placeholder `latarnia-tst.service` / `latarnia-prd.service` templates in the deployment skill with verified contents pasted from `/etc/systemd/system/` on the homeserver. P-0004 stays `[IN PROGRESS]` until this lands.

### Acceptance Criteria

- **env-prefix:** With `ENV=tst`, `ServiceManager` produces unit names like `latarnia-tst-example_full_app.service`. With `ENV=prd`, it produces `latarnia-prd-...`. With `ENV` unset or `ENV=dev`, it produces `latarnia-dev-...`.
- **env-validation:** An unrecognized `ENV` value (e.g. `ENV=staging`) logs a warning and falls back to `dev`.
- **tests:** All `tests/unit/test_service_manager.py` tests pass against the new prefix shape.
- **bootstrap-docs:** A new engineer following the deployment skill can stand up `latarnia-tst.service` and `latarnia-prd.service` on a fresh Pi without asking questions — every file path, user, working directory, and command is spelled out.
