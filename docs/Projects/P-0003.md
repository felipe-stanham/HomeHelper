# P-0003: Dynamic MCP Port Allocation

**Spec:** [P-0003/spec.md](P-0003/spec.md) | **Created:** 2026-04-10 | **Status:** [ ] Not Started

## Scope Summary

| # | Scope | Branch | Caps | Status |
|---|-------|--------|------|--------|
| 1 | MCP port infra | scope/P-0003-1-mcp-port-infra | cap-002, cap-003 | [ ] |
| 2 | Launch integration | scope/P-0003-2-launch-integration | cap-001, cap-004, cap-005 | [ ] |
| 3 | Example app + docs | scope/P-0003-3-example-app-docs | cap-006 | [ ] |

---

## Scope 1: MCP port infra

**Branch:** `scope/P-0003-1-mcp-port-infra`
**Status:** `[ ]`
**Depends on:** none

### Tasks
- [ ] Add `MCPPortRange` model to `config.py` with `start=9001, end=9099` defaults
- [ ] Add `mcp_port_range` field to `ProcessManagerConfig`
- [ ] Add `mcp_port_range` block to `config/config.json`
- [ ] Extend `PortManager` with `allocate_mcp_port(app_id)` and `release_mcp_port(app_id)` methods
- [ ] Add `app_mcp_ports: Dict[str, int]` tracking dict alongside `app_ports`
- [ ] Update `get_port_statistics()` to include MCP port counts
- [ ] Add unit tests for MCP port allocation/release/statistics
- [ ] Add MCP port range tests to `TESTS.md`

### Acceptance Criteria

- **cap-002:** `config.json` has a new `mcp_port_range` block with `start` and `end`. Defaults: `start=9001, end=9099`. TST and PRD ranges are documented in `SYSTEM.md`.
- **cap-003:** `PortManager.allocate_mcp_port(app_id)` returns a free port from the MCP range. `PortManager` tracks MCP ports separately from REST ports. `get_port_statistics()` includes MCP allocation counts.

---

## Scope 2: Launch integration

**Branch:** `scope/P-0003-2-launch-integration`
**Status:** `[ ]`
**Depends on:** Scope 1

### Tasks
- [ ] Remove `mcp_port` field from `AppConfig` model entirely
- [ ] Remove any code that reads `manifest.config.mcp_port` (AppManager discovery populating `MCPInfo`)
- [ ] Update `ProcessManager.start_app()` to call `allocate_mcp_port()` for MCP-enabled apps and pass `--mcp-port` in the command
- [ ] Update `ProcessManager.stop_app()` to call `release_mcp_port()` on shutdown
- [ ] Store allocated MCP port in `MCPInfo.mcp_port` at launch time
- [ ] Update `ServiceManager.generate_service_template()` to include `--mcp-port` in `ExecStart`
- [ ] Verify `MCPGateway` already reads from `MCPInfo.mcp_port` (no changes expected)
- [ ] Verify `HealthMonitor` MCP probe already reads from `MCPInfo.mcp_port` (no changes expected)
- [ ] Update existing unit tests for ProcessManager and ServiceManager
- [ ] Add tests to `TESTS.md` for MCP port launch integration

### Acceptance Criteria

- **cap-001:** `AppConfig` has no `mcp_port` field. Apps with `mcp_server: true` get a port allocated automatically. Any manifest with `mcp_port` is rejected at validation.
- **cap-004:** When starting an MCP-enabled app, the command includes `--mcp-port <port>`. The allocated MCP port is stored in `MCPInfo.mcp_port`.
- **cap-005:** `MCPGateway` connects to the dynamically allocated port (no behavior change needed if `MCPInfo.mcp_port` is already populated correctly).

---

## Scope 3: Example app + docs

**Branch:** `scope/P-0003-3-example-app-docs`
**Status:** `[ ]`
**Depends on:** Scope 2

### Tasks
- [ ] Remove `mcp_port` from `examples/example_full_app/latarnia.json`
- [ ] Verify `example_full_app/app.py` reads MCP port from `--mcp-port` CLI arg (it does)
- [ ] Update `docs/System/app-specification.md` to document MCP port allocation (manifest declares capability, platform assigns port)
- [ ] Add MCP port ranges to the environment isolation table in `docs/SYSTEM.md`
- [ ] Run full integration test: start platform, start example app, verify MCP tools reachable via gateway
- [ ] Update `TESTS.md` with integration test for dynamic MCP port allocation
