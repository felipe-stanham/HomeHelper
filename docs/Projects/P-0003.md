# P-0003: Dynamic MCP Port Allocation

**Spec:** [P-0003/spec.md](P-0003/spec.md) | **Created:** 2026-04-10 | **Status:** [IN PROGRESS]

## Scope Summary

| # | Scope | Branch | Caps | Status |
|---|-------|--------|------|--------|
| 1 | MCP port infra | scope-P-0003-1-mcp-port-infra | cap-002, cap-003 | [DONE] |
| 2 | Launch integration | scope-P-0003-2-launch-integration | cap-001, cap-004, cap-005 | [DONE] |
| 3 | Example app + docs | scope-P-0003-3-example-app-docs | cap-006 | [DONE] |

---

## Scope 1: MCP port infra

**Branch:** `scope-P-0003-1-mcp-port-infra`
**Status:** `[DONE]`
**Depends on:** none

### Tasks
- [x] Add `MCPPortRange` model to `config.py` with `start=9001, end=9099` defaults
- [x] Add `mcp_port_range` field to `ProcessManagerConfig`
- [x] Add `mcp_port_range` block to `config/config.json`
- [x] Extend `PortManager` with `allocate_mcp_port(app_id)` and `release_mcp_port(app_id)` methods
- [x] Add `app_mcp_ports: Dict[str, int]` tracking dict alongside `app_ports`
- [x] Update `get_port_statistics()` to include MCP port counts
- [x] Add unit tests for MCP port allocation/release/statistics
- [x] Add MCP port range tests to `TESTS.md`

### Acceptance Criteria

- **cap-002:** `config.json` has a new `mcp_port_range` block with `start` and `end`. Defaults: `start=9001, end=9099`. TST and PRD ranges are documented in `SYSTEM.md`.
- **cap-003:** `PortManager.allocate_mcp_port(app_id)` returns a free port from the MCP range. `PortManager` tracks MCP ports separately from REST ports. `get_port_statistics()` includes MCP allocation counts.

---

## Scope 2: Launch integration

**Branch:** `scope-P-0003-2-launch-integration`
**Status:** `[DONE]`
**Depends on:** Scope 1

### Tasks
- [x] Remove `mcp_port` field from `AppConfig` model entirely
- [x] Remove any code that reads `manifest.config.mcp_port` (AppManager discovery populating `MCPInfo`)
- [x] Update `ProcessManager.start_app()` to call `allocate_mcp_port()` for MCP-enabled apps and pass `--mcp-port` in the command
- [x] Update `ProcessManager.stop_app()` to call `release_mcp_port()` on shutdown
- [x] Store allocated MCP port in `MCPInfo.mcp_port` at launch time
- [x] Update `ServiceManager.generate_service_template()` to include `--mcp-port` in `ExecStart`
- [x] Verify `MCPGateway` already reads from `MCPInfo.mcp_port` (no changes expected)
- [x] Verify `HealthMonitor` MCP probe already reads from `MCPInfo.mcp_port` (no changes expected)
- [x] Update existing unit tests for ProcessManager and ServiceManager
- [x] Add tests to `TESTS.md` for MCP port launch integration

### Acceptance Criteria

- **cap-001:** `AppConfig` has no `mcp_port` field. Apps with `mcp_server: true` get a port allocated automatically. Any manifest with `mcp_port` is rejected at validation.
- **cap-004:** When starting an MCP-enabled app, the command includes `--mcp-port <port>`. The allocated MCP port is stored in `MCPInfo.mcp_port`.
- **cap-005:** `MCPGateway` connects to the dynamically allocated port (no behavior change needed if `MCPInfo.mcp_port` is already populated correctly).

---

## Scope 3: Example app + docs

**Branch:** `scope-P-0003-3-example-app-docs`
**Status:** `[DONE]`
**Depends on:** Scope 2

### Tasks
- [x] Remove `mcp_port` from `examples/example_full_app/latarnia.json`
- [x] Verify `example_full_app/app.py` reads MCP port from `--mcp-port` CLI arg (it does)
- [x] Update `docs/System/app-specification.md` to document MCP port allocation (manifest declares capability, platform assigns port)
- [x] Add MCP port ranges to the environment isolation table in `docs/SYSTEM.md`
- [ ] Run full integration test: start platform, start example app, verify MCP tools reachable via gateway
- [x] Update `TESTS.md` with integration test for dynamic MCP port allocation
