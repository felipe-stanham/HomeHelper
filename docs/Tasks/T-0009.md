# T-0009: Close P-0003 — mark `[DONE]` with the Scope 3 integration test waived

**Status:** [DONE]
**Branch:** `task-T-0009-close-p-0003`
**Created:** 2026-08-06

## Context

T-0008 established that `P-0003` (Dynamic MCP Port Allocation) was legitimately
`[IN PROGRESS]`: all three scopes landed, but one Scope 3 task was never ticked —

> `- [ ] Run full integration test: start platform, start example app, verify MCP
> tools reachable via gateway`

and under CLAUDE.md ("Testing") an unexecuted test is what blocks `[DONE]`.

The user has decided to close the project without running it ("close P-0003, no
need to run anything now"). This task records that decision and applies it.

## Deviation from the project rules — recorded deliberately

CLAUDE.md states:

> Tests must be executed, not reviewed. No scope or task can be marked `[DONE]`
> based on code review alone.
> Never mark a Scope `[DONE]` unless its tests have been **executed and passed**.

P-0003 is being closed against that rule by explicit user decision. The waiver is
recorded rather than papered over:

- The Scope 3 checkbox is left **unchecked** and annotated inline as waived. It is
  not ticked, because ticking it would assert an execution that never happened.
- A "Closure Note" in `P-0003.md` states what was and was not verified.

What *is* verified: the unit-test layer for MCP port allocation is green and
covered by seven entries in `TESTS.md` ("MCP Port Management" — allocation within
range, exhaustion, release, reuse, independence from REST ports, statistics, plus
manifest rejection and service-template emission). What is **not** verified is the
end-to-end path — platform up, example app started, MCP tools actually reachable
through the gateway on a dynamically allocated port.

The residual risk is therefore integration-level only: a wiring defect between
`allocate_mcp_port` and the gateway's live SSE connection would not have been
caught by the unit suite. `test_mcp_dynamic_port_allocation` remains in `TESTS.md`
(the platform integration section), so it stays in the regression suite and will
be exercised on the next full integration run — closing P-0003 does not delete
the coverage, it only stops blocking on it now.

## Scope

Documentation only. No `src/` change, no test change.

- `docs/Projects/P-0003.md` — header `[IN PROGRESS]` → `[DONE]`; annotate the
  waived Scope 3 task; add the Closure Note.
- `docs/SYSTEM.md` — Projects row `[IN PROGRESS]` → `[DONE]`.
- `docs/Projects/INDEX.md` — move P-0003 from **Active** to **Completed**.

`TESTS.md` needs no edit: the critical-path criterion is already promoted there.

Promotion triggers re-checked: no scope decomposition, one concern, no data-model
change, no new external service — stays a Task.

## Implementation Plan

- [x] **1. Mark `P-0003.md` `[DONE]`** with the waiver annotated, not hidden.
- [x] **2. Update `docs/SYSTEM.md`** Projects row.
- [x] **3. Move P-0003 to Completed** in `docs/Projects/INDEX.md`.
- [x] **4. Verify three-way agreement** across all project rows.
- [x] **5. Commit and merge to `dev`.**

## Acceptance Criteria

- **ac-01:** `docs/Projects/P-0003.md` header status cell reads `[DONE]`.
- **ac-02:** `docs/SYSTEM.md` Projects row for P-0003 reads `[DONE]`.
- **ac-03:** `docs/Projects/INDEX.md` lists P-0003 exactly once, under
  `## Completed`, as `[DONE]`.
- **ac-04:** The Scope 3 integration-test checkbox is still `[ ]` and carries the
  waiver annotation — the file does not claim the test passed.
- **ac-05:** For every project ID in `docs/SYSTEM.md`'s Projects table, the status
  agrees across `docs/SYSTEM.md`, `docs/Projects/INDEX.md`, and the project's own
  `P-xxxx.md` header.
- **ac-06:** `test_mcp_dynamic_port_allocation` is still present in `TESTS.md`.
- **ac-07:** No file under `src/` or `tests/` is modified.

## Notes

The second defect T-0008 reported is untouched and still open: `P-0010.md`'s
per-scope task checkboxes are all `[ ]` and its per-scope `**Status:**` lines still
hold the unedited template ladder, even though the project is `[DONE]`. Cosmetic,
but it makes the file read as unfinished.
